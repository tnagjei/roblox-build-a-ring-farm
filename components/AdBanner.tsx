// input: optional advertising placement
// output: disabled ad container to avoid visible placeholder text in development and production
// pos: components/AdBanner.tsx（更新规则：广告供应商变更或ID变更时修改此文件）

"use client";

export default function AdBanner() {
  return null;
}
